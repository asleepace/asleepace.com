---
import BlogItem from './BlogItem.astro'
import clsx from 'clsx'

interface Props {
  className?: string
  posts: Blog[]
}

const { posts, className } = Astro.props
---

<ul class={clsx('flex flex-1 gap-4', className)}>
  {posts.map(({ data, id }) => <BlogItem data={data} slug={id} />)}
</ul>
