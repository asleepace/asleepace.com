# Add this at the http level (usually in /etc/nginx/nginx.conf or at the top of your site config)
# If you're behind Cloudflare, load balancers, or other proxies, add their IP ranges here
# set_real_ip_from 127.0.0.1;           # Trust localhost
# set_real_ip_from 10.0.0.0/8;          # Trust private networks
# set_real_ip_from 172.16.0.0/12;       # Trust private networks  
# set_real_ip_from 192.168.0.0/16;      # Trust private networks
# real_ip_header X-Forwarded-For;       # Use X-Forwarded-For header
# real_ip_recursive on;                 # Look through proxy chain

server {
        client_max_body_size 64M;
        server_name asleepace.com www.asleepace.com;
        
        # special config for stream/events
        # Match any path containing 'events' or 'stream'
        location ~ /(events|stream) {
                proxy_set_header Connection '';
                proxy_http_version 1.1;
                proxy_buffering off;
                proxy_cache off;
                proxy_read_timeout 24h;
                proxy_pass http://localhost:4321;
        }

        # route for rust backend
        #location /api/ {
        #        proxy_pass http://localhost:3030;
        #        proxy_set_header Host $host;
        #        proxy_set_header X-Real-IP $remote_addr;
        #        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        #}

        # route for astro
        location / {
                proxy_pass             http://localhost:4321;
                proxy_read_timeout     60;
                proxy_connect_timeout  60;
                proxy_redirect         off;

                # Allow the use of websockets
                proxy_http_version 1.1;
                proxy_set_header Upgrade $http_upgrade;
                proxy_set_header Connection 'upgrade';
                proxy_set_header Host $host;
                proxy_cache_bypass $http_upgrade;

                # Forward IP and protocol information
                proxy_set_header Host $host;
                proxy_set_header X-Real-IP $remote_addr;
                proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
                proxy_set_header X-Forwarded-Proto $scheme;
                proxy_set_header X-Forwarded-Host $host;
        }


    listen 443 ssl; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/asleepace.com/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/asleepace.com/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot


}
server {
    if ($host = www.asleepace.com) {
        return 301 https://$host$request_uri;
    } # managed by Certbot


    if ($host = asleepace.com) {
        return 301 https://$host$request_uri;
    } # managed by Certbot


        listen 80;
        server_name asleepace.com www.asleepace.com;
    return 404; # managed by Certbot
}